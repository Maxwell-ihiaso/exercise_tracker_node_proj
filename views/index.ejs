<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Exercise Tracker</title>
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <header>
      <h1>Exercise Tracker</h1>
    </header>

    <main>
      <div class="container">
        <form action="/api/users" method="POST" class="new_user">
          <h2>Create a New User</h2>
          <p><span> POST /api/users</span></p>
          <input type="text" placeholder="username" name="username" required />
          <input type="submit" value="Submit" />
        </form>

        <form id="exercise_form" method="POST" class="add_exercise">
          <h2>Add Exercise</h2>
          <p><span> POST /api/users/:_id/exercises</span></p>
          <input id="uid" type="text" placeholder="_id" name="_id" required />
          <input
            id="desc"
            type="text"
            placeholder="decription*"
            name="description"
            required
          />
          <input
            type="number"
            placeholder="duration (mins)*"
            name="duration"
            required
          />
          <input
            id="date"
            type="date"
            placeholder="date (yyyy-mm-dd)"
            name="date"
          />
          <input type="submit" value="Submit" />
        </form>
      </div>

      <div class="documentation">
        <h2>
          GET user's exercise log:
          <span>GET /api/users/:_id/logs?[from][&to][&limit]</span>
        </h2>
        <p><b>[ ]</b> = optional</p>
        <p><b>from, to</b> = dates (yyyy-mm-dd); <b>limit</b> = number</p>
      </div>
    </main>

    <script src="/index.js"></script>
  </body>
</html>
